---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64 

steps:
- name: greeting
  commands:
  - echo ${DRONE_BUILD_NUMBER}
  - echo $HOME  
- name: maven
  commands:
  - export HOME=/root
  - mvn clean package -DskipTests -Dmaven.test.skip=true -Dgpg.skip
 